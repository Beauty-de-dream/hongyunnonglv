/* pages/machinery-detail/machinery-detail.wxss */

.page-detail {
  padding-bottom: 160rpx;
}

.detail-image {
  width: 100%;
  height: 480rpx;
}

.detail-info {
  padding: 30rpx;
  background: #fff;
}

.detail-name {
  font-size: 38rpx;
  font-weight: bold;
  color: #333;
  display: block;
  white-space: normal;
  word-wrap: break-word;
  line-height: 1.4;
  flex: 1;
  min-width: 0;
}

.price-row {
  margin-top: 16rpx;
  display: flex;
  align-items: baseline;
}

.detail-price {
  font-size: 48rpx;
  font-weight: bold;
  color: #4CAF50;
}

.detail-desc {
  font-size: 28rpx;
  color: #666;
  margin-top: 16rpx;
  line-height: 1.6;
  display: block;
}

.detail-card {
  background: #fff;
  margin: 20rpx 24rpx;
  border-radius: var(--radius-md);
  padding: 30rpx;
  box-shadow: var(--shadow-md);
}

.card-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
  display: block;
  margin-bottom: 24rpx;
  padding-bottom: 20rpx;
  border-bottom: 1rpx solid #eee;
  white-space: normal;
}

.info-list {
  display: flex;
  flex-direction: column;
}

.info-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16rpx 0;
  border-bottom: 1rpx solid #f5f5f5;
}

.info-item:last-child {
  border-bottom: none;
}

.info-label {
  font-size: 28rpx;
  color: #666;
}

.info-value {
  font-size: 28rpx;
  color: #333;
  font-weight: 500;
}

.info-value.link {
  color: #1976D2;
}

/* 底部操作栏 */
.bottom-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  display: flex;
  padding: 20rpx 24rpx;
  padding-bottom: calc(20rpx + env(safe-area-inset-bottom));
  background: #fff;
  box-shadow: 0 -4rpx 16rpx rgba(0, 0, 0, 0.08);
  z-index: 100;
}

.bottom-btn-left {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 0 30rpx;
}

.bottom-icon {
  font-size: 36rpx;
}

.bottom-label {
  font-size: 22rpx;
  color: #666;
}

.bottom-btn-right {
  flex: 1;
  height: 88rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #4CAF50;
  color: #fff;
  font-size: 32rpx;
  font-weight: bold;
  border-radius: 44rpx;
  margin-left: 20rpx;
}

.bottom-btn-right.disabled {
  background: #bbb;
}
